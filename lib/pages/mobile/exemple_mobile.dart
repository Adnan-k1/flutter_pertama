
import 'package:flutter/material.dart';

class ExempleMobile extends StatelessWidget {
  
  
  const ExempleMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("ini mobile"),),
      body: Center(child: Text("contoh mobile"),),
    );
  }
}